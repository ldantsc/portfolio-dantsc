---

import ProjectsCard from '../projects-card/projects-card.jsx'
import styles from '../../styles/projects.module.css'

/* API PUBLICA DO GITHUB */
const user = await fetch("https://api.github.com/users/ldantsc/repos", { headers: { authorization: import.meta.env.API_TOKEN}})
const userData = await user.json()
/* Filtrando o json para trazer apenas os repositórios que não tenha a tag study-notes */
/* Após devolver o array filtrado para newArr, utilizar o map para percorrer o json */

const newArr = userData.filter((data) => {
    if(!data.topics.includes("study-notes")){
        return data
    }
})

---

<div id="container-projects" class="">
    <div id="container-projects-title" class="my-20 text-center">
        <h2 data-aos="fade-up" class="title">Meu projetos</h2>
        <p>Esta sessão contém alguns dos meus projetos pessoais ou profissionais
            realizados durante o meu aprendizado/carreira como desenvolvedor. Utilize o botão "saiba mais" para conferir
            no meu repositório do Github (link do deploy no repositório)
        </p>
        <p class="italic pt-3">Observação: pode conter projetos em andamento</p>
    </div>
    <div id="container-projects-card" class=`flex flex-wrap gap-5 justify-center`>
        
        {newArr.map((data) => {
            const img = `https://raw.githubusercontent.com/ldantsc/${data.name}/${data.default_branch}/demo.png` 
            return (
                <ProjectsCard title={data.name} description={data.description} url={data.html_url} image={img} client:only/>
            )
        })}
    </div>
    <div class="py-20">
        <p class="italic text-end">Aguarde para mais projetos no futuro...</p>
    </div>
</div>